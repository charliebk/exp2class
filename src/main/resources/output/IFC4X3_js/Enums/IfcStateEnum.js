// ENUM TYPE IfcStateEnum
const IfcStateEnum = Object.freeze({
    LOCKED: "LOCKED",
    READONLY: "READONLY",
    READONLYLOCKED: "READONLYLOCKED",
    READWRITE: "READWRITE",
    READWRITELOCKED: "READWRITELOCKED"
});
